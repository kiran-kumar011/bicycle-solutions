import React from 'react';
import Header1 from './header1';

const NavBar = ({}) => {
  return (
    <header style={{ width: '100vw' }}>
      <Header1 />
    </header>
  );
};

export default NavBar;
